## v1.12.0 - 🎉 适配 QQ 官方机器人平台 2026/9/11

### 新增功能
- **QQ 官方机器人（qq_official）支持** — 指令、定时推送、链接识别、翻译、截图与媒体发送均可正常使用；`metadata.yaml` 声明平台支持并同步更新 README

### 改进
- **Nitter 镜像列表更新** — 内置镜像加入可用的 `nitter.fuyuyuki.top`，并按其可用性排序，失效的 `nitter.net` 保留为兜底
- **合并转发按平台自动降级** — qq_official / qq_official_webhook 不支持合并转发组件，插件会自动改用普通消息链发送，避免推文被静默丢弃
- **图片预下载与文字优先** — qq_official 上图片会在构建消息链时预下载为内联字节（失败自动重试一次），并把「文字+图片」拆成先文字后图片的多条独立消息，规避平台富媒体消息把图片渲染在文字上方的问题
- **WebUI 平台提示** — 对无法获取群列表的平台（如 qq_official）给出「请使用 /推特关注 指令新增订阅」的明确提示

### 修复
- **qq_official 含图片推文发送失败** — 适配器内部下载远程图片无重试、连接截断时整条消息失败；现在插件先行预下载并降级为内联字节
- **合并转发导致推文永久丢失** — 部分平台丢弃合并转发组件却仍推进游标；现在不支持合并转发的平台自动降级为普通消息

---

## v1.11.0 - ✨ 翻译阻塞保护与截图头像缓存 2026/9/5

### 新增功能
- **截图头像缓存** — 截图模式会将主推文和引用帖头像缓存到 AstrBot 插件数据目录，并以内嵌图片渲染；缓存可跨插件重载复用，最多保留 200 项、总计 16 MiB

### 改进
- **轮询翻译降级** — 同一 Provider 连续两条推文翻译失败后，本轮剩余推文直接使用原文，下一轮自动重新尝试；`/推特测试` 和链接识别保持独立，不参与轮询失败计数

### 修复
- **翻译异常导致轮询长时间阻塞** — 修复 LLM Provider 或其上游代理持续重试时，轮询可能长期停在翻译阶段、集体转发缓存迟迟无法发送的问题

---

## v1.10.1 - 🔧 修复 FxTwitter 启动失败后轮询不恢复 2026/8/18

### 修复
- **启动健康检查失败后无法自愈** — FxTwitter 初始健康检查失败时仍会启动后台恢复任务，后续按照轮询间隔重新检查；数据源恢复后立即执行一轮订阅检查，并从原有游标继续补发积压推文，无需再次重载插件


---

## v1.10.0 - ✨ 推文链接按需解析与 Provider 下拉选择 2026/7/29

### 新增功能
- **推文链接解析三态开关** — 将 `twitter_link_recognition_enabled` 调整为支持自动解析、完全关闭和仅指令触发三种模式，原有布尔值 `true`、`false` 会分别映射为自动解析和完全关闭，无需重新配置。

### 改进
- **翻译 Provider 下拉选择** — 翻译Provider配置项改用AstrBot原生选择器

---

## v1.9.0 - ✨ Dashboard 订阅管理与轮询可靠性 2026/7/20

### 新增功能
- **插件WebUI** — 基于 AstrBot Plugin Pages 新增 Dashboard 内嵌管理页面，可按群聊查看订阅，并新增、修改或移除推主，要求Astrbot版本 ≥ v4.24.2
- **每轮推送数量限制** — 新增 `twitter_poll_max_tweets_per_user`，默认每个推主每轮最多推送 5 条；积压推文按旧到新分轮补发，避免数据源恢复后集中刷屏

### 改进
- **代码轻量重构** — 将订阅存储、消息构建、消息发送和轮询处理从 `main.py` 拆分至独立服务，降低主类复杂度并方便后续维护

### 修复
- **逐条提交轮询进度** — 普通模式下每成功处理一条推文便立即保存游标，插件重载或容器重启时不再整批重新推送
- **切换数据源后重复推送** — 在 Nitter 与 FxTwitter 之间切换并重载插件时，会跳过已经处理的帖子，同时继续补发真正尚未发送的积压内容
- **发送失败错误推进游标** — 主要消息和降级发送结果会反馈至轮询层；发送失败时保留当前游标，下次轮询继续重试
- **集体转发提前推进游标** — 集体转发改为实际发送完成后再按推主提交候选游标，发送失败时不会越过尚未送达的推文
- **集体转发去重记录写入过早** — 只有对应消息成功发送后才记录转帖去重状态，失败时不会误判为已发送

---

## v1.8.0 - ✨ 新增 FxTwitter API 数据源 2026/7/15

### 新增功能
- **FxTwitter JSON API** — 新增 `twitter_data_provider=fxtwitter`，支持用户资料、时间线、指定推文、图片、视频、转帖、引用帖与敏感内容适配
- **有限 cursor 分页** — 时间线最多抓取有限页数，并在本地完成 `since_id` 筛选、去重与旧到新排序

### 兼容与稳定性
- **Nitter 保持默认** — 默认仍为 `nitter`，旧扁平配置、分组配置、KV 键和订阅数据格式不变
- **独立健康状态** — FxTwitter 模式不检测、不访问或切换 Nitter 镜像
- **防御性错误处理** — 复用异步客户端，识别超时、429、5xx 和非法 JSON；详情失败时不越过未处理的 `since_id`

---

## v1.7.1 - 🔧 优化代理环境下的媒体加载 2026/7/12

### 新增功能
- **代理预下载媒体** — 新增配置项 `twitter_pre_download_media`，默认关闭；配置代理并开启后，插件会通过代理预先下载图片，避免网络受限环境下媒体加载超时

### 改进
- **截图模式媒体加载** — 作者头像、推文图片及视频封面会经代理下载并以内嵌数据渲染，改善截图中媒体缺失或渲染失败的问题

---

## v1.7.0 - ✨ 新增媒体资源独立发送开关 2026/6/30

### 新增功能
- **媒体资源独立发送开关** — 新增配置项 `twitter_send_media_separately`，默认开启；关闭后不再在正文或截图之外追加原图、视频及视频降级链接，截图中的媒体预览仍会保留

---

## v1.6.0 - ✨ 新增转帖推文去重

### 新增功能
- **轮询转帖去重** — 新增配置项 `twitter_deduplicate_retweets`，默认关闭；开启后同一条原帖被多个订阅推主转发时，每个会话只推送第一次。去重只影响轮询推送中的转帖，不影响正常推文和引用帖、`/推特测试` 和链接识别

---

## v1.5.0 - ✨ 支持自定义翻译提示词

### 新增功能
- **自定义翻译提示词开关** — 新增 `twitter_translate_custom_prompt_enabled`，开启后可使用用户自定义的翻译 system prompt

---

## v1.4.1 - 🔧 修复普通消息模式媒体问题

### 修复
- **非合并转发自动推送丢图片** — 修复关闭合并转发消息时，自动轮询推送带图片推文只发送文字的问题
- **视频推文普通消息显示异常** — 修复关闭合并转发消息时，`/推特测试` 和链接识别遇到视频推文可能不显示正文的问题

---

## v1.4.0 - ✨ 新增截图模式

### 新增功能
- **X 风格截图模式** — 新增配置项 `twitter_text_render_mode`，支持将推文正文渲染为 X 官方浅/暗色时间线风格截图，默认仍为普通文字推送
- **截图主题配置** — 新增 `twitter_screenshot_theme`，支持 `dark` / `light` 两种截图主题，默认保持暗色风格
- **视频大小保护** — 新增 `twitter_video_max_size_mb`，默认 256 MB，视频超限时改为发送说明和视频链接


## v1.3.0 - ✨ 新增转帖、引用帖与配置分组增强

### 新增功能
- **转帖推送开关** — 新增配置项 `twitter_include_retweets`，控制轮询推送和 `/推特测试` 是否包含转帖；关闭后轮询跳过转帖，测试指令会向后寻找最新非转贴推文，默认开启
- **推送帖子链接开关** — 新增配置项 `twitter_include_tweet_link`，推文消息是否附带 X/Twitter 帖子链接，默认开启

### 改进
- **转帖消息格式** — 转帖会显示“转发者 转发 原作者 了帖子”，随后发送被转发帖子的正文与媒体资源

---

## v1.2.0 - ✨ 新增图片质量配置与修复

### 新增功能
- **推文图片质量配置** — 新增配置项 `twitter_image_quality`，支持选择图片质量
  - `orig`（默认）：原图 .jpg，通过 Nitter `/pic/orig/` 路由获取
  - `large`：缩略图 .webp
### 修复

- **直播推文导致视频无法播放** — 检测推文链接中是否包含 `/i/broadcasts/`，识别为直播时自动过滤该推文的所有视频和图片内容

### 改进
- **清理冗余回退代码** — 移除不会触发的回退分支（`main-tweet` 容器回退、推文正文宽松选择器、`IndexError` 不可达捕获等），代码更简洁

---

## v1.1.0 - 🔧 重构与修复

### 修复
- **`/推特测试` 和链接识别不处理视频** — 视频组件此前被混入 Node 内发送，可能导致超时；现在与轮询推送一致，视频独立发送，失败回退为链接
- **纯文本模式下 screen_name 显示不一致** — 当昵称与用户名相同时，此前纯文本模式会冗余显示 `@user (user)`，现在统一只显示 `@user`

### 改进
- **提取公共方法，消除重复逻辑** — 新增 `_build_nickname`、`_maybe_translate`、`_split_chain_for_nodes`、`_send_video_or_fallback` 四个工具方法，统一昵称构建、翻译调用、Node 构建和视频发送逻辑
- **纯文本模式统一使用 `_build_tweet_chain`** — 消除纯文本模式与合并转发模式的消息构建重复代码，确保行为一致
- **集体转发模式增加校验警告** — 当 `twitter_collective_forward` 开启但 `twitter_use_node` 关闭时，初始化时输出 warning 日志提醒用户

### 移除
- **移除 `plugin_enabled` 配置项** — 该"插件总开关"实际仅控制轮询，不影响指令和链接识别，容易误导；推送控制请使用 `/推特推送 <开启|关闭>` 指令
- **移除冗余方法 `get_user_timeline`**

---

## v1.0.3 - ⚡链接识别改为全局配置

### 改进
- **链接识别开关改为全局配置** — 新增配置项 `twitter_link_recognition_enabled`（默认开启），在 WebUI 插件配置页面即可控制

### 移除
- **移除 `/推特链接识别` 指令** — 链接识别开关已改为全局配置项，不再需要按群聊单独控制的指令
---

## v1.0.2 - ✨ 新增批量取关功能

### 新增功能
- **批量取关推主** — 新增 `/推特批量取关` 指令，支持一次取关多个推主，用法：`/推特批量取关 <推主ID1> <推主ID2> ...`
  - 别名：`/twitter_batch_unfollow`
  - 逐个校验订阅状态，仅移除当前会话的订阅关系，不影响其他会话
  - 执行完成后汇总展示每个推主的取关结果（成功/失败原因）

---

## v1.0.1 - 🛠️ 去除了带反爬的镜像站，只保留https://nitter.net
